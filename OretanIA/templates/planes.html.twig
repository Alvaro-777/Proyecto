{% extends 'base.html.twig' %}

{% block title %}Planes de Créditos{% endblock %}

{% block head %}
    <link rel="stylesheet" href="{{ asset('css/planes.css') }}">
{% endblock %}

{% block body %}

    <h1 class="planes-titulo">Elige tu plan</h1>

    <section class="planes-container">

        <article class="plan-card">
            <h2>Básico</h2>
            <p class="creditos"><strong>500 créditos</strong></p>
            <p class="precio">4,99 €</p>
            <a class="btn-plan" href="
            {% if logado %}
                {{ path('pago_checkout', {'plan': 1}) }}
            {% else %}
                {{ path('login') }}
            {% endif %}
        ">Comprar</a>
        </article>

        <article class="plan-card">
            <h2>Pro</h2>
            <p class="creditos"><strong>1000 + 200 créditos</strong></p>
            <p class="precio">9,99 €</p>
            <a class="btn-plan" href="
            {% if logado %}
                {{ path('pago_checkout', {'plan': 2}) }}
            {% else %}
                {{ path('login') }}
            {% endif %}
        ">Comprar</a>
        </article>

        <article class="plan-card">
            <h2>Premium</h2>
            <p class="creditos"><strong>1500 + 500 créditos</strong></p>
            <p class="precio">14,99 €</p>
            <a class="btn-plan" href="
            {% if logado %}
                {{ path('pago_checkout', {'plan': 3}) }}
            {% else %}
                {{ path('login') }}
            {% endif %}
        ">Comprar</a>
        </article>

    </section>

    <section class="planes-info">
        <p>
            Los créditos adquiridos pueden utilizarse en cualquiera de los servicios de Oretan-IA,
            incluyendo generación de audio, predicciones y chatbots inteligentes.
            <br><br>
            Los créditos no caducan y se descuentan automáticamente según el uso de cada herramienta.
            Puedes ampliar tu plan en cualquier momento desde tu perfil.
        </p>
    </section>

{% endblock %}
